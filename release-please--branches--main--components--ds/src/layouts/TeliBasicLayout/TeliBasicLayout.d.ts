import { default as React, ReactNode } from '../../../node_modules/react';

export type TeliBasicLayoutProps = Partial<{
    children: ReactNode;
}>;
declare const TeliBasicLayout: React.FC<TeliBasicLayoutProps>;
export default TeliBasicLayout;
