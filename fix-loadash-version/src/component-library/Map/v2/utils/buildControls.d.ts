import { MapControlsConfig } from '../types';
export declare const buildControls: (controls?: Partial<MapControlsConfig>, createControls?: () => import('ol').Collection<import('ol/control').Control>) => import('ol').Collection<import('ol/control').Control>;
