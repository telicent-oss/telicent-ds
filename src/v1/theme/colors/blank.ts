import { alpha } from "@mui/material/styles";

const main = "#FFF";

const BLANK = {
  50: alpha(main, 0.7),
  100: alpha(main, 0.5),
  200: alpha(main, 0.3),
  400: alpha(main, 0.2),
  500: alpha(main, 0.15),
  600: alpha(main, 0.1),
};

export default BLANK;
